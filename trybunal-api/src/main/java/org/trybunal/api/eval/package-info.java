/**
 * Pure domain types for evaluation.
 *
 * <p>Mirrors the convention of {@link org.trybunal.api.model} — every type is
 * an immutable {@code record} or a sealed interface over records. No
 * dependencies beyond the JDK and slf4j-api.</p>
 */
package org.trybunal.api.eval;
